package modelo;
/**
 * Tipos posibles de un paquete turistico, segun el enunciado del proyecto.
 * Usar enum en vez de String evita valores invalidos o mal escritos.
 */
public enum TipoPaquete {
    NACIONAL, INTERNACIONAL, AVENTURA, RELAX, CULTURAL
}